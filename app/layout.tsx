import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Yachtdrop - Marine Supplies Delivered",
  description: "Order boat supplies instantly. Live product data from nautichandler.com with fast delivery to your yacht.",
  keywords: ["marine supplies", "boat parts", "yacht accessories", "nautical equipment"],
  openGraph: {
    title: "Yachtdrop - Marine Supplies Delivered",
    description: "Order boat supplies instantly. The Uber Eats for yacht crews.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
