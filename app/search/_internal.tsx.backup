import { Suspense } from 'react';
import InternalSearchPage from './_internal';

// Wrapper that adds Suspense boundary for useSearchParams()
export default function SearchPage() {
  return (
    <Suspense fallback={<div>Loading...</div>}>
      <InternalSearchPage />
    </Suspense>
  );
}
